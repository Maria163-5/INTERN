# Project

This repository contains the project for the `intern` workspace. Main entry: `agent.py`.

How to push this project to a remote:

1. Create a remote repository on GitHub (or another host).
2. Add the remote locally: `git remote add origin <URL>`
3. Push the main branch: `git push -u origin main`
